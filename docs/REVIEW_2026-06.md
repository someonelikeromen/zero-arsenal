# Zero-Arsenal 代码缺陷复审报告（2026-06）

> 复审基准日期：**2026-06-03**
> 基线参照：`docs/STUB_ANALYSIS.md`（3 轮 151 条，经本次复审证实**已部分过时**）
> 复审方式：16 个只读子代理（C1~C16）按目录切片全量行级复审，逐条核实旧条目当前状态并发掘新增项。
> 分片明细（行级证据）：`docs/review/def_c1.md` ~ `def_c16.md`
>
> 配套报告：`docs/DESIGN_CONFORMANCE_2026-06.md`（设计符合度）、`docs/REVIEW_TODO_2026-06.md`（P0~P3 可执行清单）

---

## 一、总览统计

| 维度 | 数量 | 说明 |
|---|---|---|
| 复审分片 | 16（C1~C16） | 覆盖 agents / engine / memory / api / bus / db / extensions / tools / prompts / skills / frontend / tests / UX |
| 复审条目区块 | 约 226 | 含历史已修复确认 |
| ✅ 历史已修复（确认） | ~57 | 旧 151 条中已被 Phase 0A/后续修复 |
| 🆕 本次新发现 | ~83 | 旧报告未记录的运行期 bug / 降级 / 死代码 / 未接线 |
| 🔴 核心（P0 级） | 约 14 | 运行期崩溃 / 安全裸奔 / 核心机制空转 |

**核心结论**：
1. 旧报告中多数 P0「fail-open 安全门禁」「文件不存在」类条目**已修复**（rules/dm 已 fail-closed、`utils/llm_client.py`/`db/queries.py` 已存在、redis_bus 已实质实现、前端 UX 基础设施已接线）。
2. 但本次发掘出**一批新的运行期硬伤**——多处把 `aiosqlite.Row` 当 dict、把 `@asynccontextmanager` 当对象、相对导入越界、引用不存在的符号/表，这些被 `try/except` 静默吞掉，表现为「功能静默失效」而非显式报错。
3. **最严重的系统级空转**：4 个世界扩展 hooks 全失效、记忆 LLM 提取链生产从不触发、记忆四层召回默认只生效 1 层。

---

## 二、🔴 P0 级（仍存在 / 新发现，需优先处理）

| ID | 位置 | 问题 | 来源 |
|---|---|---|---|
| NEW-C6-01 | `api/routers/sessions.py:483-489,534` | `fork_session` 对 `aiosqlite.Row` 调 `.get()` → AttributeError，**只要会话有 parts/NPC，fork 必崩** | def_c6 |
| NEW-C6-02 | `api/routers/sessions.py:1019-1026` | 章节回滚 `create_branch=true` 分支 INSERT 用了不存在的列 `current_mode`/`forked_from`（实为 `mode`/`branch_of`）→ 500 | def_c6 |
| NEW-C6-03 | `api/routers/sessions.py:471-476` | fork 复制 messages 丢失 `content`/`message_type`/`phase`，fork 后用户消息文本为空 | def_c6 |
| NEW-C9-01 | `extensions/{muv_luv,gundam_seed,infinite_arsenal,crossover}/hooks.py` | 4 个世界 hooks 顶层 `from ..hook_protocol import BaseHook` / `from ..registry import hook_registry`——**两个符号/模块均不存在**，导入即崩溃 → 机甲损伤/PS装甲/武器耐久/跨界SP结算全部空转 | def_c9 / conf_b04 |
| NEW-C3-01 | `agents/llm.py:126,139` | `load_agent_config` 警告路径调用未定义的 `logger` → 任意未配置 agent 名触发 `NameError`（反而抵消了 T-D04/05 的修复） | def_c3 |
| NEW-C5-01 | `memory/extractor.py:330` | `emb_client.embed_batch()` 不存在（`_EmbeddingClient` 仅有 `embed`）→ 提取节点向量写入必 AttributeError | def_c5 / conf_b08 |
| NEW-C5-02 | `memory/extractor.py`、`rollback.py` | 把 `get_db()`（`@asynccontextmanager`）当对象调 `upsert_node_sync/_exec/mark_node_synced`（均不存在）| def_c5 / conf_b08 |
| NEW-C5-03 | `memory/engine.py`、`extract_queue.py` | LLM 图谱提取链（extractor.py）**生产路径从未被触发**，队列走 SQLite 正则启发式 → 向量/图召回无数据源 | def_c5 / conf_b08 |
| node_sync_status | `memory/rollback.py:67,154`、`extractor.py:341`；`db/schema.py` | 代码对 `node_sync_status` 执行 DELETE/UPDATE，但**全仓无任何 CREATE TABLE** → `no such table` | def_c5 / conf_b06 / conf_b08 |
| NEW-C1-01 | `agents/rules_agent.py:186`、`dm_agent.py:260` | verdict 字段缺失/未知值时回落 `pass` → 两道门禁在「可解析但语义缺失」路径上 **fail-open**（D-01/D-02 修好的解析失败路径的缺口） | def_c1 |
| R-D01 | `api/middleware/auth.py:36-44` | 未配置 `ZERO_ARSENAL_API_TOKEN` 时**完全放行所有 /api/**（仅 WARNING），生产误配即裸奔 | def_c7 |
| D-16(残留) | `tools/registry.py:234-242` | ask 路径已 fail-closed ✅，但 `_resolve_permission` 异常仍回落工具自带默认（多为 allow）= 残留 fail-open | def_c11 / conf_b07 |
| NEW-B10-01 | `agents/permission.py:159-187`、`profiles/review.yaml` | review 模式 `style_check`/`purity_check` 不匹配任何 allow pattern，落到 `*→deny` → **审校核心工具被自身规则禁用** | conf_b10 |
| NEW-C2-03 | `agents/chronicler_agent.py:36-45,183-197` | `start_message_id` 从不写入 → 阈值后**每回合重复固化 + 章节膨胀 + 重复 LLM 摘要** | def_c2 |

> Redis 模式专属（仅设置 `REDIS_URL` 时触发，否则降级进程内队列）：
> - **B09 缺陷1**（🔴）：`EventType` 被当 Enum 调用（实为普通类），连真 Redis 后 `_redis_to_queue` 构造事件即抛错被吞 → **订阅者收不到任何事件，多进程能力名存实亡**（`bus/redis_bus.py:142,205`）。
> - **NEW-C7-05**：publish 不携带 event id/timestamp，Last-Event-ID 锚点错位 → 断线续传退化为「回溯 1h 全量重放」。

---

## 三、🟡 降级 / 未接线 / 死代码（P1~P2，按切片）

### C1 Agent 安全门禁
- ✅ 已修复：STUB-03（rules LLM 失败→block）、D-01（dm 解析失败→block）、D-02（rules 解析失败→block）。
- ⚠️ T-D06 permission 导入失败→全工具归 general 组（过度限制）；T-D07/NEW-C1-02 注释漂移（超时实为 deny）+ rules_agent 对同步 `log_roll` 误用 `await` 致预检骰子不归档。

### C2 叙事 Agent
- ✅ D-07 chronicler 摘要失败已有降级。⚠️ D-04/D-05/D-06/D-08 各类降级（世界事件清空/原文透传/状态丢失）仍在，多缺日志。
- 🆕 NEW-C2-01 NPC 空文本回退固定台词；NEW-C2-02 style 失败丢弃程序化纯净度结果；NEW-C2-04 章节摘要跨章重叠；NEW-C2-05 world_agent 零日志排障盲区。

### C3 Agent 基础设施
- ✅ STUB-T01 gacha 字段已闭环、M-08 已加日志。🔄 D-09/D-22/T-D04/T-D08/T-D10 已从静默改为带日志降级。
- ⚠️ T-D09 options 失败仅 DEBUG、T-D11 注入节点缺失静默跳过、M-07/M-09 死代码、M-10 dotenv 静默。
- 🆕 NEW-C3-02 ask 超时 PendingAsk 泄漏；NEW-C3-03 compaction 只追加摘要不裁剪反增 token；NEW-C3-04 tool_loop 顶层吞异常产空文本；NEW-C3-05 dm_verdict 默认值游离枚举；NEW-C3-06 冗余 import。

### C4 引擎
- ✅ STUB-R03（改抛错）、STUB-R09（三级回退）。`dice.py`/`psyche.py` 确认无占位。
- ⚠️ STUB-R04 vm 无 RestrictedPython 静默返回原 state；R-M15 `CombatRoundResult` 全项目无引用（整轮战斗结算未暴露）；R-D09 jinja2 非依赖→模板原文直出。
- 🆕 NEW-C4-01 vm 缺 guard 函数 → 状态变更脚本恒静默失效（即使装了 RestrictedPython）；NEW-C4-02 world_events 键错位（world_time/location 永不来自 world_agent）；NEW-C4-03 dice 减值 schema 错位（伤势/压力减值恒 0）；NEW-C4-04 同步 DB 回退空操作。

### C5 记忆全栈
- ✅ STUB-R05（consolidator LLM 真可用）、STUB-R07（retriever import 真修）、R-M12（字段对齐）。
- 🔄 STUB-R06/R08「表面修复」（import 不报错但运行期崩，见 P0）；STUB-02 vector.py 非桩但受可选依赖门控。
- ⚠️ R-M13 `synopsis_to_arc` 死配置；R-D11 `get_extract_queue` 符号缺失靠脆弱双 import 回退；R-D12 队列启发式分段非 LLM。
- 🆕 NEW-C5-04 retriever 无词法-only 兜底；NEW-C5-05 adapter.add_memory 向量写入路径全错（属性/方法/构造参数均不符，被吞）；NEW-C5-06 冗余 import + UUID 尾4位标题。

### C6 API 路由
- ✅ STUB-R01（fork 列名已修）、STUB-T02（PATCH /sessions 已实现）、R-D05（返回真实 character）、R-D06（回滚失败 fail-loud）、R-M02（动态检测 hooks）。
- ⚠️ R-D02/R-D03/R-D04/R-D08 各类静默降级（兜底默认卡/空列表/except pass/0 条提炼）。
- 🆕 见 P0（NEW-C6-01/02/03）+ NEW-C6-04 角色快照恢复静默 pass、NEW-C6-05 `create_world_archive` 把 world_key 误写为 archive_type。

### C7 中间件 + 总线 + DB
- 🔄 redis_bus 已实质实现（仅 `:39/:68` 残留「桩未填充」过时文案）。
- ⚠️ R-D01（见 P0）、R-M01 限流进程内存多 worker 不共享、R-M03 迁移失败静默、R-M16/R-M17 订阅计数 -1 / event_log 持久化失败静默、R-M14 character_v4 未用 jsonschema 校验。
- 🆕 NEW-C7-01 令牌桶字典无淘汰（DoS）、NEW-C7-02 无条件信任 X-Forwarded-For（限速可绕过）、NEW-C7-03 token 非常量时间比较、NEW-C7-04 RedisEventBus pubsub/task 泄漏、NEW-C7-05（见 P0）、NEW-C7-06 Redis 计数 -1、NEW-C7-07 schema_version 按列表下标编号易错位、NEW-C7-08 init_db 吞建表致命错误、NEW-C7-09 每事件新开 DB 连接、NEW-C7-10 auth 排除路径文档不一致。

### C8 扩展框架
- ✅ M-01（废弃函数）、M-07（Protocol `...`）、T-D02（不适用）。🔄 STUB-09（get_rules_skills 已覆写，get_character_template 仍死）、T-D01（on_demand 入参已加但 3 个调用方都不传）。
- ⚠️ M-02 overlay 失败静默 pass + 重复堆叠、T-D03 YAML 回退脆弱解析。
- 🆕 NEW-C8-01/02 `_template` 工具/钩子泄漏进运行时、NEW-C8-03 内置钩子双重注册、NEW-C8-04 hook/rules 只扫内置目录忽略 user/project 三级、NEW-C8-05 无默认值插件自动实例化必失败、NEW-C8-06 盲目实例化模块内所有类、NEW-C8-07 `enabled:false` 回退失效、NEW-C8-08 用不存在的 `.key`。
- `_template` 骨架本身可直接复制使用。

### C9 世界扩展
- ✅ STUB-04/06/07/08、T-D20（spar 已加骰子）。各扩展完整度：wuxia ~95%（唯一 hooks 生效）、infinite_arsenal/crossover ~75%、muv_luv/gundam_seed ~65-70%、web_scraper ~70%。
- ⚠️ T-D14 web_scraper 无 session/world 时静默不写库却返回 ok:True、T-D19/NEW-C9-02 forge_weapon LLM 导入 4 点越界恒降级、STUB-T11 web_scraper 缺 manifest 未进注册表、gundam_seed get_skill_catalog 扫描不存在的 skills/。
- 🆕 NEW-C9-01（见 P0）、NEW-C9-03 manifest provides 与实际矛盾、NEW-C9-04 wuxia 钩子双重注册、NEW-C9-05/06 并行死代码与无调用方 register_* 函数、NEW-C9-07 muv_luv/gundam_seed 缺 data/ 目录。

### C10 数据文件
- ✅ T-D29~T-D33（pool/acg/shop/sects/techniques catalog 均已真正加载）。
- ⚠️ T-D34=T-M17 agents.json 的 `memory_extract` 死配置（提取器实际用 `role="dm"`）、T-M16 muv_luv 缺 display_name。
- 🆕 NEW-C10-01 `crossover/data/character-template.json`、NEW-C10-02 `crossover/data/world-registry.json` **两个死数据文件**全仓 0 引用。

### C11 工具层
- ✅ D-16 ask 路径 fail-closed。🔄 D-17 部分改善。
- ⚠️ D-10~D-15 全转为「LLM/DB 优先 + 兜底降级」，主风险是兜底伪造数据掩盖真实失败（占位选项/奇幻商品/品质公式估价/硬编码 NPC 边界/关键词技能触发/默认 pool=2）+ R-M06 skill 注册失败静默。
- 🆕 NEW-C11-01/02 NPC 三处存储不一致（spawn 的 NPC 查不到）、NEW-C11-03 文档矛盾、NEW-C11-04 硬编码 deepseek、NEW-C11-05 两套 llm_complete 来源、NEW-C11-06 roll_check `difficulty` 死参数、NEW-C11-07 SSE message_id 不一致、NEW-C11-08 skill 条件裸 eval、NEW-C11-09 MCP 静态注册 schema 失真。

### C12 Prompts + Skills
- ✅ R-D13（condition 失败改 return False）。⚠️ R-D14/R-D15/R-M04/R-M05/R-M10（多为可接受降级，但 jinja2 非依赖致 .j2 恒不生效）。
- 🆕 NEW-C12-01 Layer1 片段双源、agents/*.md 覆盖 core_prompts（narrator_p4 格式分叉）、NEW-C12-02 TokenBudget 裁剪无调用方传预算从未生效、NEW-C12-03 VALID_PHASES 等常量死且漏 rules/style、NEW-C12-04 `registry.build()` 规范入口无人调用、NEW-C12-05 watcher 只刷 Tool 不刷 Hook/Agent、NEW-C12-06 裁剪 off-by-one、NEW-C12-07 直写私有 `_skills`、NEW-C12-08 condition eval 注入面。
- Layer0 完整；Layer1 对 npc/world/var/chronicler 不完整。

### C13 前端 stores + lib
- ✅ T-D15/D16/D17/D19/M04/M05（deleteSession 走 apiFetch、NPC/物品导入接线、loadMessages 调用、theme 持久化、dice history 接线、5 个死 API 已接线）。
- ⚠️ 仍死 13 项：`character.activeSkills`、3 个快照 action（T-M03）、world store `loadNpcs/npcs/addArchive`（T-D18+NEW-C13-02）、`api.listNpcs/createNpc/updateNpc/deleteNpc`（NEW-C13-01 重复死方法）、`api.getChapters/getParts`（NEW-C13-04 被裸 fetch 旁路）。
- 🆕 NEW-C13-03 stores 多处裸 fetch 绕过 apiFetch 统一错误处理。

### C14 前端 组件 + 页面
- ✅ 9 条已修复（STUB-T02/T03、T-D20~24、M-04、T-M01）。🔄 T-D25（时序脆弱残留）、T-M02（正规重定向）。⚠️ T-M15 虚拟滚动仍未实现。
- 🆕 NEW-C14-01 createSession 类型缺字段靠 as 强转、**NEW-C14-02 发送后 10s 无条件解锁输入（长生成并发重发风险）**、NEW-C14-03 PromptManager 未按 sort_order 排序、NEW-C14-04 WorldModal 仅以 worldId=null 创建（已有世界无法重跑抓取）、NEW-C14-05 自动滚底仅依赖 parts.length。

### C15 测试质量
- ✅ STUB-T04/T05/T06（真实 HTTP 集成 / 名实相符 / 退出码已修）。🔄 STUB-T07/08/09（已加运行时用例但保留静态桩）、T-D26/T-D27（browser_e2e 已修，infinite_arsenal `?query=` 仍错、429 仍当通过）。
- ⚠️ STUB-T10 测试内重写业务逻辑、T-D28 Playwright 缺失假绿、M-09/T-M13 软跳过、**T-M12 test_live_generation 无 stub marker + 无后端跳过 → CI `-m "not stub"` 会被收集报错**。
- 🆕 NEW-C15-01 test_round10_quick.py `t1_~t7_` 命名永不被收集（死测试）、NEW-C15-02 test_p2_extension_loader 静态检查未标 stub。
- stub marker 已在 `pytest.ini:5` 与 `backend/pyproject.toml:48-49` 双处注册并生效；7 个伪测试已正确标注，3 个应标未标。

### C16 UX / 新手体验
- ✅ P0-1 Toast、P0-2 统一确认框、P0-4 LLM 配置 Banner、主题持久化、文风选择器、First Message 自动触发、输入框状态机均已真正接线。
- 🔄 P0-3 危险操作内联 Warning 仅在确认弹窗内（缺按钮旁静态红字）、P1-1 会话创建仍单页表单非 4 步向导。
- ⚠️ **P0-5 Hub 会话 Tab 仍未合并列表+创建**（创建在「会话」Tab、历史列表在「存档」Tab，IA 割裂）。

---

## 四、历史已修复附录（旧 STUB_ANALYSIS 过时条目）

经本次复审确认**旧报告已过时**的代表性条目（详见各分片）：
- STUB-R05~R08 称 `utils/llm_client.py`、`db/queries.py` 不存在 → **现已存在**（R05/R07 真修，R06/R08 import 修但运行期仍崩）。
- STUB-03 rules「LLM 不可用默认放行」桩 → **已改 fail-closed（block）**。
- redis_bus「此实现桩未填充」→ **publish/subscribe/get_events_after_from_db 均已实质实现**（仅残留过时文案）。
- STUB-R01 fork NPC 列名 `npc_key` → **已改正确列 `key`**。
- T-D29~D33 数据文件「内联未读」→ **均已真正从 JSON 加载**。
- T-M05 前端 6 个死 API、T-D15~D24 多项未接线 → **大部分已接线**。
- 前端 Toast/确认框/主题/文风/LLM Banner「零接线」→ **已接线生效**。

> **建议**：`docs/STUB_ANALYSIS.md` 应标注为「历史快照（截至 Phase 0A 前）」，后续以本报告为准。

---

## 五、按严重度的处置优先级

详见 `docs/REVIEW_TODO_2026-06.md`（P0~P3 可勾选总清单）。概要：
- **P0**：本报告第二节 14 项（运行期崩溃 / 安全 / 核心空转）。
- **P1**：记忆系统重建（向量/图写入链路 + chromadb 依赖）、4 世界 hooks 修复、jinja2 列为依赖、vm guard、NPC 单一存储、play/plan/review 权限对齐、角色卡 v4 对齐（见符合度报告）。
- **P2**：各类降级补日志、死代码清理、测试 stub 标注补齐、前端死字段清理。
- **P3**：文档同步（设计文档大面积滞后，见符合度报告）、注释漂移修正、optimize 项。
