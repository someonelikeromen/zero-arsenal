# Zero-Arsenal 复审 · 待裁定决策清单（2026-06）

> 复审基准日期：**2026-06-03**
> 来源：`docs/REVIEW_2026-06.md` + `docs/DESIGN_CONFORMANCE_2026-06.md` + `docs/REVIEW_TODO_2026-06.md`
>
> 本清单只收录**需要你拍板方向**的事项——即靠纯技术判断无法单方面决定的取舍（设计与实现冲突、改代码 vs 改文档、是否投入重建等）。
> 已明确的 bug（P0 崩溃/安全）不在此列，直接照 TODO 修即可。
>
> 每条格式：**背景/冲突** → **选项** → **影响** → **建议**。请在每条 `裁定：____` 处填写你的选择。

---

## 〇、最高层 · 一个总方向先定

### D0. `docs/design/` 与实现冲突时，谁是权威？
- 背景：设计文档自称「唯一需求与架构权威来源」，但复审发现**实现普遍优于/超出文档**（端点更多、工具更多、SSE 有续传增强等），大量「偏离」其实是**文档滞后**而非代码错。
- 选项：
  - **A（推荐）：代码为实然基准** —— 默认「改文档对齐代码」，仅当代码确实违背设计意图（如权限放宽）时才改代码。
  - B：设计为应然基准 —— 凡偏离一律改代码回归设计（工作量极大，且会回退掉已有增强）。
  - C：逐条人工裁定（最慢）。
- 影响：决定下面所有「改代码 vs 改文档」类条目的默认处置。
- 建议：**A**。下面各条在 A 前提下给默认值，你只需挑出例外。
- 裁定：________

---

## 一、安全/权限策略（影响行为，必须你定）

### D1. play 模式默认权限：`allow` 还是 `ask`？
- 冲突：`10-permission-modes` §2「实现差异对照」写 play=allow，§3「原始设计意图」写 play=ask（消费类工具需确认）。**文档自相矛盾**，实现取了 allow。
- 选项：A 维持 play 全 allow（沉浸优先，少打断）；B 回归设计：edit/earn/purchase/fork/mcp_* 改 ask 确认（防误操作）。
- 影响：play 体验「顺滑 vs 安全」；并决定 `profiles/play.yaml` 与 §2/§3 哪边改。
- 建议：消费类（earn/purchase/edit/fork）→ **ask**，纯叙事/读取 → allow（折中）。
- 裁定：________

### D2. plan 模式是否允许 `roll_*` 掷骰？
- 冲突：设计「plan 绝不掷骰」，实现 `profiles/plan.yaml` 允许 roll_*。
- 选项：A 禁止（回归设计）；B 允许（更新文档）。
- 建议：**A 禁止**（plan 的定位就是只读推演）。
- 裁定：________

### D3. deny 安全底线能否被扩展 overlay 覆盖？
- 背景：当前 overlay 可覆盖 deny，`extensions/plugin.py:183` 还会就地污染全局 Profile。
- 选项：A deny 为硬底线、overlay 只能收紧不能放开（推荐）；B 允许扩展放开 deny（灵活但有风险）。
- 建议：**A**（这是安全语义，倾向硬性；属策略决定故列出）。
- 裁定：________

### D4. 未配置 `ZERO_ARSENAL_API_TOKEN` 时的默认行为？
- 背景：当前未配 token = 完全放行所有 /api/（仅 WARNING）。
- 选项：A 默认 fail-closed（无 token 则拒绝/仅本地放行）；B 维持放行但启动时强 WARNING + 文档标注（仅限单机本地部署场景）。
- 建议：**A** 或「仅 127.0.0.1 放行、外部拒绝」。涉及部署形态，需你确认本项目是否纯单机。
- 裁定：________

---

## 二、记忆系统：是否投入重建（工作量大，需立项决定）

### D5. 「四层混合召回」是兑现还是降级为文档？
- 背景：设计承诺 向量65%+Bigram35%+图扩散+认知权重 四层；实际默认环境**只生效 1 层**（缺 chromadb + 生产从不写图/向量，LLM 提取链是死代码）。
- 选项：
  - **A：兑现** —— 声明 chromadb/sentence-transformers 依赖、接通 LLM 提取+embedding+图写入链路（对应 P0 NEW-C5-01/02/03 + P1 一批）。工作量大但落地核心卖点。
  - B：官方降级 —— 明确「默认只做认知权重+词法召回」，向量/图标为可选高级特性，更新设计文档 08，删/标注死代码。
- 影响：决定 P1 是否新增一个「记忆重建」里程碑。
- 建议：先做 **B 的兜底**（保证默认环境召回可用、不崩），再按资源决定是否推进 A。
- 裁定：________

### D6. viewer_agent 多视角隔离：实现五视角还是接受布尔可见性？
- 背景：设计要五视角隔离，实现退化为布尔可见性。
- 选项：A 实现五视角；B 文档降级为布尔可见性。
- 建议：跟随 D5——若做 A（重建）则一并实现，否则改文档。
- 裁定：________

---

## 三、数据模型 / 角色卡（影响存档结构，需你定）

### D7. 角色卡升到 v4，还是把设计降回 v3？
- 背景：设计是 v4（meta/identity/energy_pools/loadout/psychology-OCEAN/economy-badges/tier/achievements/6 部位 body_parts），实现是 **v3 简化卡**（5 维属性、4 部位 hp_ratio）。
- 选项：
  - A：实现 v4 全字段（功能更丰富，但要做迁移 + 前端表单 + 各扩展适配，工作量大）。
  - B：设计降回 v3，明确精简卡为正式 schema。
  - C：分阶段——先补高价值字段（energy_pools/loadout/identity），OCEAN/achievements/badges 延后。
- 影响：DB schema、迁移、前端 CharacterCreator、各世界扩展。
- 建议：**C**（按玩法价值排序补字段）。需你确认哪些字段是必须。
- 裁定：________

### D8. CHECK 约束 / 外键 ON DELETE 是否补齐？
- 背景：多数表缺 CHECK 与外键级联，设计未强制但隐含。
- 选项：A 补齐（数据更健壮，迁移有风险）；B 维持从简并在文档注明。
- 建议：新表补、老表谨慎（避免迁移破坏现有存档）。
- 裁定：________

---

## 四、Agent / 管线架构（偏离设计，改代码 vs 改文档）

### D9. Narrator P3/P4 变量职责：谁负责写变量块？
- 背景：设计要 P4 专责 `<VariableBlock>`，实现是 **P3 内联 `{{SET}}`**（职责与设计相反）。
- 选项：A 回归设计（P4 专责）；B 维持 P3 内联并更新文档。
- 影响：叙事管线提示词与变量提取时机。
- 建议：需你定——若无强约束，倾向 **B 更新文档**（当前实现能跑）。
- 裁定：________

### D10. 图拓扑：npc/world 合并单节点 gather，还是拆回两节点？
- 背景：设计是 npc、world 两节点，实现合并为 `parallel_nw` 单节点用 asyncio.gather。
- 选项：A 维持合并（性能/简洁，更新文档）；B 拆回（贴合设计图）。
- 建议：**A**（合并是合理优化）。
- 裁定：________

### D11. AgentProfile 多模型分工：实现还是改文档？
- 背景：设计按角色分配不同模型，实现全用 deepseek-chat。
- 选项：A 接通多模型映射（成本/质量可调，需配置）；B 文档改为单模型现状。
- 建议：保留 A 的能力但默认单模型——即**接通映射、默认值全 deepseek**，兼顾。
- 裁定：________

### D12. tool_loop 最大迭代 `MAX_ITER`：10 还是设计的 20？
- 选项：A 改回 20（贴合设计）；B 维持 10（更省）。
- 建议：小事，倾向 **B 改文档为 10**（除非你遇到过工具链被截断）。
- 裁定：________

### D13. 外部 MCP 子 Agent（`call_external_agent` + `asyncio.shield`）是否本期实现？
- 背景：设计 §8.2/8.3 有，代码无。
- 选项：A 本期补；B 标注为「未来特性」延后。
- 建议：**B 延后**（除非有明确用例）。
- 裁定：________

---

## 五、事件总线 / SSE（文档内部矛盾，需先裁基准）

### D14. `to_sse` 载荷结构：嵌套 data 还是展开？
- 冲突：设计 §3.1 示例展开字段，实现是嵌套 `data`。前端已按嵌套消费。
- 选项：A 维持嵌套并改文档（不动前端，推荐）；B 改成展开（要同步改前端）。
- 建议：**A**。
- 裁定：________

### D15. `get_subscriber_count` 等接口：同步还是异步？
- 冲突：09 §2.1 接口写同步，§2.2 样例写异步。需统一。
- 建议：按实现现状统一文档（多为同步），无需改代码。
- 裁定：________（默认：按实现统一文档）

---

## 六、Prompt / Skill / 工具（取舍）

### D16. 8 个预置 Skill 目录：补齐还是从设计删除？
- 背景：设计列了 8 个预置 Skill，`backend/skills/` 实际无任何 SKILL.md。
- 选项：A 按设计补写 8 个 Skill；B 从设计删除/标注为示例。
- 建议：需你定内容价值——若这些 Skill 是卖点则 A，否则 **B**。
- 裁定：________

### D17. SKILL.md 格式：补齐 frontmatter（id/role/source/version）+ 五节正文，还是简化设计？
- 选项：A 回归完整格式；B 文档采用当前简化格式。
- 建议：跟随 D16。
- 裁定：________

### D18. condition 表达式：上 AST 白名单沙箱，还是接受裸 `eval`？
- 背景：prompt/skill 条件用裸 `eval`（注入面）。这是**安全项**，倾向必做，但实现方式（自研 AST 白名单 vs 引入 `simpleeval` 等库）需你选。
- 选项：A 自研 AST 白名单；B 引入轻量库（如 simpleeval，加依赖）。
- 建议：**B simpleeval**（成熟、省事），除非你不想加依赖。
- 裁定：________

### D19. 工具超时常量对齐到哪套？
- 冲突：实现 15/10/60/5s，设计 30/120/15/300s。
- 选项：A 用设计值（更宽松，长工具不易超时）；B 维持实现值改文档。
- 建议：MCP/长任务类倾向用设计的较大值，其余维持——需你确认是否遇到过超时误杀。
- 裁定：________

---

## 七、前端技术选型（改造成本，需你定）

### D20. 是否引入 shadcn/ui？
- 背景：设计要 shadcn/ui，实现全手写 Tailwind。
- 选项：A 引入（统一组件库，但要重构现有组件）；B 维持手写改文档。
- 建议：**B**（已能跑，引入收益有限），除非你计划大改 UI。
- 裁定：________

### D21. Tailwind 维持 v3 还是升 v4？
- 选项：A 升 v4（贴合设计，有 break change）；B 维持 v3 改文档。
- 建议：**B**。
- 裁定：________

### D22. IndexedDB 驱逐策略：实现 LRU 还是接受时间过期？
- 选项：A LRU（贴合设计）；B 维持时间过期。
- 建议：**B**（差异不大），除非有缓存膨胀问题。
- 裁定：________

---

## 八、文档与产物管理

### D23. `docs/review/` 27 份证据分片：保留还是删除？
- 背景：plan 收尾要求清理临时分片，但三份最终报告把它们当作**行级证据附录**引用；本仓非 git，删除不可逆。我已删除纯脚手架 `_FORMAT.md`，保留了 27 份证据分片。
- 选项：A 保留作证据层（推荐）；B 全部删除（同步移除报告中的引用）。
- 建议：**A 保留**。
- 裁定：________

### D24. `STUB_ANALYSIS.md` 去留？
- 背景：已加「历史快照（已部分过时）」横幅并指向新报告。
- 选项：A 保留作历史；B 删除。
- 建议：**A 保留**（有历史追溯价值）。
- 裁定：________

---

## 裁定速览（✅ 已裁定 · 2026-06-04）

| 编号 | 主题 | 我的建议 | **你的裁定** | 落地方向 |
|---|---|---|---|---|
| D0 | 权威基准 | 代码为实然 | **A** | 默认改文档对齐代码；仅违背设计意图处改代码 |
| D1 | play 权限 | 消费类→ask | **A** | 维持 play 全 allow（沉浸优先），改文档消除 §2/§3 矛盾 |
| D2 | plan 掷骰 | 禁止 | **A** | 禁止 plan 掷骰，改 `profiles/plan.yaml` 移除 roll_* |
| D3 | deny 可否被覆盖 | 硬底线 | **B** | 允许扩展 overlay 放开 deny（灵活），但须修就地污染全局 Profile |
| D4 | 无 token 行为 | fail-closed | **A** | 无 token → fail-closed（拒绝/仅本地放行） |
| D5 | 四层召回 | 先兜底再重建 | **A** | 兑现四层：声明 chromadb 依赖 + 接通 LLM 提取+embedding+图写入 |
| D6 | viewer 视角 | 跟随 D5 | **A** | 实现五视角隔离 |
| D7 | 角色卡 v4 | 分阶段补 | **A** | 实现 v4 全字段 |
| D8 | CHECK/外键 | 新表补 | **A（现无存档）** | 全表补 CHECK/外键——因当前无存档可放心迁移 |
| D9 | P3/P4 变量职责 | 倾向改文档 | **A** | 回归设计：P4 专责 `<VariableBlock>` |
| D10 | 图拓扑合并 | 维持合并 | **A** | 维持 `parallel_nw` 合并节点，改设计文档图 |
| D11 | 多模型分工 | 接通+默认单模型 | **A** | 接通多模型角色映射 |
| D12 | MAX_ITER | 改文档为 10 | **A** | 改回 20（贴合设计） |
| D13 | 外部 MCP 子 Agent | 延后 | **A** | 本期实现 `call_external_agent` + `asyncio.shield` |
| D14 | to_sse 结构 | 维持嵌套 | **A** | 维持嵌套 data，改设计文档 §3.1 |
| D15 | 接口同步/异步 | 按实现统一文档 | **A** | 维持同步（本进程计数），文档注明 |
| D16 | 8 个预置 Skill | 看价值 | **A（做完整测试用）** | 补写 8 个预置 Skill，作完整端到端测试用例 |
| D17 | SKILL.md 格式 | 跟随 D16 | **A** | 回归完整格式（frontmatter + 五节正文） |
| D18 | condition 沙箱 | simpleeval | **B** | 引入 simpleeval 轻量库替换裸 eval |
| D19 | 超时常量 | 部分用设计值 | **A** | 全部用设计值 30/120/15/300s |
| D20 | shadcn/ui | 维持手写 | **B** | 维持手写 Tailwind，改设计文档 |
| D21 | Tailwind 版本 | 维持 v3 | **A** | 升级 Tailwind v4（贴合设计，处理 break change） |
| D22 | IndexedDB 驱逐 | 维持时间过期 | **A** | 实现 LRU 驱逐 |
| D23 | review 分片去留 | 保留 | **A（+ 本地 git）** | 保留 27 份证据分片；并为本仓初始化本地 git 记录 |
| D24 | STUB_ANALYSIS 去留 | 保留 | **A（标注历史）** | 保留，并明确标注为「历史状态参考」 |

> 说明：多条裁定选择了「实现/对齐设计」而非「改文档」（D5/D6/D7/D9/D11/D12/D13/D16/D17/D19/D21/D22），意味着本期工作量偏向**补实现**而非仅更新文档。下一步可据此把 `REVIEW_TODO_2026-06.md` 的 P1/P3 重新排期。
